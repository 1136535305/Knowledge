<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:background="#FFFFFF">

    <ImageView
        android:id="@+id/iv_atatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginLeft="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_launcher_round" />

    <TextView
        android:id="@+id/tv_editor_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginTop="-8dp"
        android:text="星玫"
        android:textColor="#000000"
        app:layout_constraintStart_toEndOf="@+id/iv_atatar"
        app:layout_constraintTop_toTopOf="@+id/iv_atatar"
        tools:text="星玫" />

    <TextView
        android:id="@+id/tv_bio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="-8dp"
        android:layout_marginStart="12dp"
        android:text="产品设计师 @华兴资本"
        app:layout_constraintBottom_toBottomOf="@+id/iv_atatar"
        app:layout_constraintStart_toEndOf="@+id/iv_atatar"
        tools:text="产品设计师 @华兴资本" />

</android.support.constraint.ConstraintLayout>